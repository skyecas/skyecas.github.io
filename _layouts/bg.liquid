<!doctype html>
<html lang="{{ site.lang }}">
  <head>
    {% include head.liquid %}
  </head>
  <body style="padding-bottom: 0px !important;">
    <canvas class="background" id="bgCanvas"></canvas>
    <div class="content container mt-5" role="main" style="">{{ content }}</div>
  </body>
</html>
